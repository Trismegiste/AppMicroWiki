{% extends 'base.html.twig' %}
{%  block title %}
    Listing
{%  endblock %}
{% block body %}
    <header>
        <h1>Listing</h1>
        <a href="{{ path('app_documentcrud_new') }}">New document</a>
    </header>
    <section>
        <ul>
            {% for doc in listing %}
                <li>
                    <a href="{{ path('app_documentcrud_show',{title: doc}) }}">{{ doc }}</a>
                </li>  
            {% endfor %}
        </ul>
    </section>
{% endblock %}